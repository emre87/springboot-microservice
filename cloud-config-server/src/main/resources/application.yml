server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: file:///C:\PROJELER\microservice\spring-microservices\config-file
          clone-on-start: true